function run(input) {
    let i = input;

    do {
        console.log('hit');
    }
    while(false) {
        i++
        console.log(i);
    }

}

run(5);

// doWhileLoop, atleast running the code once even 
// if it evaluates false
